name: Main

on: [push, pull_request]

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: psf/black@stable
        with:
          options: "--verbose --diff --exclude migrations --exclude frontend"