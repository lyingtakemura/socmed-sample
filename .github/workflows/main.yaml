name: main

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: black . --verbose --diff --exclude /migrations/
        uses: psf/black@stable
        with:
          options: "--verbose --diff --exclude /migrations/"